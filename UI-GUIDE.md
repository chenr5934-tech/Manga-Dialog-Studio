# 界面按钮速查

按区域列出每个按钮的作用。界面分四大块：顶部工具栏、左侧预设栏、中间画布、右侧属性栏与胶片栏。

顶部只保留项目级操作，所有工具都归到左侧工具栏里。

---

## 一、左侧工具栏

按用途分成三组，位置固定：

| 分组 | 按钮 | 作用 |
| --- | --- | --- |
| **扣选** | **矩形** | 在画布上拖拽扣出矩形分镜。开启后画布锁定，已有分镜与气泡不可拖动 |
|  | **多边形** | 单击逐个取点，满 3 点后可按 Enter、点「完成闭合」或回到起点收口 |
|  | **圆形** | 同样拖拽取景，画出来的是椭圆分镜，用于圆形取景 |
|  | **底图色** | 整页底色，**默认白色且铺满编辑区**（有原稿底图时，原稿铺在底色之上） |
| **保存** | **导入图片** | **按顺序多选导入漫画原稿**，图片进入「已导入图片」素材库，**不自动生成页面** |
|  | **导出** | 内容区切换成导出面板，见下方 |
| **辅助** | **Agent** | 把右侧栏切换成 Agent 对话面板。它自带六项漫画排版技能（分格节奏 / 构图取景 / 气泡布局 / 视线引导 / 留白呼吸 / 参考图复刻），按需加载；每轮回复会写明启用了哪些 |
|  | **模板** | 打开**整册版式模板**小窗：保存当前排版，或从模板新建后只换画面 |
|  | **贴纸** | 内容区切换成贴纸，底部变成「导入自定义贴纸」。贴纸**单击加到画布中央，也可以拖到画布指定位置** |
|  | **对话框预设** | 内容区切换成预设卡片，底部变成「导入自定义对话框」 |
| **已导入图片** | 内容区列出用过和导入过的所有图片（去重）。列表来自两处：项目目录 `uploads/` 里的常驻副本，加上当前项目正在用的图——所以把胶片栏的页面删掉，原稿也不会跟着消失。拖到**分镜**上会问是否放进该格子，拖到**空白处**则在上层新建一张可调大小的图片。鼠标移到缩略图上会出现 **抠图**、**铺满**、**移除** 三个按钮；顶端还有多选管理，见下方小节 |

点「对话框预设」「贴纸」「导出」时，下方**内容区**跟着换：

| 点谁 | 内容区 | 内容区底部 |
| --- | --- | --- |
| 对话框预设 | 内置与自定义的对话框卡片，单击加到画布，也可拖到画布指定位置 | **导入自定义对话框**、编辑填字区、新建预设 |
| 贴纸 | 四个内置分组共 26 个贴纸，外加「自定义」分组 | **导入自定义贴纸**、打开文件夹 |
| 已导入图片 | `uploads/` 里的常驻原稿 + 项目里用过的所有图片，自动去重；每张缩略图悬停后有 **抠图** / **铺满** | — |
| 导出 | 导出 PNG / PDF / ZIP 与图片倍率 | — |

### 导出面板

| 按钮 | 输出 |
| --- | --- |
| **导出 PNG（当前页）** | 当前这一页一张 PNG |
| **导出 PDF（全部页）** | 全部页合并成一个 PDF，按页面顺序 |
| **导出图片 ZIP（全部页）** | 全部页各一张 PNG，打包成一个 ZIP，包内按 001、002 编号 |
| **1x / 2x** | ZIP 导出的图片倍率。2x 更清晰、文件更大 |
| **导出未完成作品** | 把当前所有改动（含图片与编辑历史）打包成一个 `.openkoma.json` 下载，下次用「加载项目」接着改 |

---

## 二、顶部

| 按钮 | 作用 |
| --- | --- |
| **漫画对话工坊** | 产品名，非按钮 |
| **项目名输入框** | 改项目名，会影响导出文件名 |
| **分镜模式 / 对话编辑** | 切换工作流。切到对话编辑会自动收起扣选工具，避免画布停在锁定态 |
| **撤销 / 重做** | 逐步回退或恢复编辑。快捷键 Ctrl+Z / Ctrl+Shift+Z |
| **亮 / 暗** | 切换浅色／深色主题，选择会被记住 |
| **布局** | 展开面板：画布尺寸（9 个常用档 + 自定义，改完点「应用画布」）、新建分镜（新页面默认不含分镜）、椭圆分镜、添加图片层、切割画布、16 倍数吸附、选中分镜二次切割 |
| **批量样式** | 展开面板：把**圆角 / 倒角**、边框粗细等样式一次性应用到全部分镜 |
| **更多** | 展开面板：另存为、加载项目、预设库 |
| **保存项目** | 保存整册项目（含图片）。首次会让你选存放位置 |

---

## 三、左侧栏：对话框预设

| 按钮 | 作用 |
| --- | --- |
| **导入自定义对话框** | 左侧栏最醒目的按钮。选一张对话框图片（PNG 透明底最佳），在图上框出填字区域，存成预设反复套用 |
| **编辑填字区** | 先在画布上选中一个气泡，再点这里调整它的填字区域、字体与不透明度 |
| **预设卡片** | **单击**加到画布中央；**拖到画布**上放到指定位置 |
| **保存预设** | 把当前整套自定义预设写进项目目录的 `presets/`，只问一个文件名 |
| **预设库** | 打开预设库小窗：浏览文件夹里的预设文件，可载入或删除，也能下载 JSON |

> 预设是**单个对话框的外观模板**（底图、填字区、字体、不透明度），不含文字内容。
> 整册排版请用顶部的「模板」。

---

## 四、中间画布区

| 控件 | 作用 |
| --- | --- |
| **- / 缩放滑块 / +** | 调整画布显示比例（10% – 100%），只影响显示，不影响导出 |
| **扣选提示条** | 取景时出现在画布上方，说明当前状态，右侧有 **退出扣选** |
| **Enter 提示条** | 多边形取满 3 点后出现，含 **完成闭合** 与 **重来** |
| **选中手柄** | 选中分镜、气泡或图片层后出现。拖角缩放、拖边改尺寸、顶部手柄旋转（分镜按 5° 吸附） |
| **顶点手柄** | 多边形分镜的每个顶点都是可拖拽的，拖动即可改形状 |

---

## 五、右侧：属性检查器

选中什么就显示什么，内容随选中对象变化。

| 对象 | 可调项 |
| --- | --- |
| **分镜** | 坐标与尺寸、倾斜角度、边框粗细与颜色、**圆角 / 倒角**（共用 Radius）、**内边距**（边框与内容之间露出的底色）、**图像来源**（导入图片、打开手动裁剪、**去除同色背景**）、多边形分镜另有 **转为矩形分镜** |
| **已导入图片缩略图** | 悬停后出现 **抠图**（打开去背景窗口）与 **铺满**（铺满画布），见下方「抠图 / 铺满」小节 |
| **图片层 / 贴纸** | 位置与尺寸（X / Y / 宽 / 高）、旋转、**不透明度**，底部可删除。贴纸另有 **贴纸颜色** 区块，10 个常用色加取色器。两者都浮在分镜之上、气泡之下，不受分镜形状裁剪 |
| **气泡** | 内容文字、形状、背景、排版方向、字体字号颜色、文字描边、**尺寸滑条**、**不透明度**、填字区域 |

选中任意对象后，面板底部都有**层级顺序**（置顶 / 上移 / 下移 / 置底）和 **删除选中内容**（等同于按 `Delete`）。 |

### 手动裁剪窗口

| 控件 | 作用 |
| --- | --- |
| **拖拽内部** | 移动取景框 |
| **四角方块手柄** | 自由缩放取景框（保持分镜比例） |
| **四边圆点手柄** | 单独推动某条边 |
| **回到当前裁剪** | 恢复到已保存的裁剪位置 |
| **匹配比例最大区域** | 一键取最大可用区域 |
| **清除裁剪** | 移除裁剪，恢复原图完整显示 |
| **应用裁剪** | 写入并关闭 |

### 去除同色背景

| 控件 | 作用 |
| --- | --- |
| **自动取色** | 采样图片四边猜出底色，也可以自己点取色器改 |
| **容差** | 0–180。底色有渐变或压缩噪点时往大调 |
| **边缘羽化** | 0–60。调大让边界过渡柔和，减少锯齿 |
| **原图 / 结果对照** | 左右并排，透明处用棋盘格衬底，改参数即时刷新 |
| **应用到分镜** | 写入当前分镜，原底色区域变成透明 |

这个窗口有**两个入口**，用的是同一套参数和同一份预览：从分镜属性面板进，按钮是「应用到分镜」；从「已导入图片」栏的缩略图进，按钮是「抠图并放到画布」。

---

### 抠图 / 铺满 / 移除（已导入图片栏）

鼠标移到「已导入图片」里的任意缩略图上，底部浮出三个按钮。三个都不改动原图，`Ctrl+Z` 可退。

| 按钮 | 作用 |
| --- | --- |
| **抠图** | 就地打开去背景窗口，源图就是这张缩略图。确认后，去完背景的透明图作为一个新图片层落到画布中央 |
| **铺满** | 打开铺满窗口，把这一张图铺满整张画布 |
| **移除** | 只把这张从列表里拿掉，不影响画面，随时能用「恢复已移除」找回 |

抠图产出的图片层和别的图片层一样会被收进图片池，处理完立刻能在「已导入图片」里再次取用——想抠好几轮就往回叠。

### 选择性删除（已导入图片栏顶部）

列表不能乱删：里面既有 `uploads/` 里的原稿副本，也有**当前项目正在引用**的图（底图、分镜图、图片层）。后者要是真删掉，画面就缺一块了。所以分成两个动作：

| 入口 | 对原稿副本 | 对项目正在用的图 |
| --- | --- | --- |
| 缩略图上的 **移除** | 只从列表拿掉，副本还在 `uploads/` | 只从列表拿掉，画面不动 |
| 顶部 **多选** → **删除选中** | 从 `uploads/` 里彻底删掉，找不回来 | 仍然只做移除，画面不动，提示里会说明有几张是这种情况 |

顶部工具条：

| 控件 | 作用 |
| --- | --- |
| **多选 / 退出多选** | 切换管理模式。进入后缩略图左上角出现勾选框，单击整张图即勾选 |
| **全选 / 全不选** | 一次勾上全部，或全部取消 |
| **删除选中(N)** | 删除已勾选的图。没勾选时按钮是灰的 |
| **恢复已移除(N)** | 把之前**移除**过的图重新显示出来。只对「移除」有效，用「删除选中」删掉的副本找不回来 |

两条容易踩的规则：删掉的图刷新后不会自己回来；但**重新导入同一张图**会当作「我又要用了」，撤销它之前的移除记录，直接出现在列表里。

#### 铺满窗口

参数分三组，互不挤在一起。

| 分组 | 控件 | 作用 |
| --- | --- | --- |
| **铺满方式** | **拉伸铺满** | 拉满画布，图片会被压扁或拉长，不留白 |
| | **等比填满** | 保持比例放大到盖住画布，超出的部分裁掉 |
| | **等比适应** | 保持比例缩到整张图都看得见，四周留白 |
| **对齐** | **居中 / 靠上 / 靠下 / 靠左 / 靠右** | 决定裁切时保留哪一部分、或留白留在哪一侧。拉伸铺满时自动置灰 |
| **尺寸** | 原图与画布的比例对照 | 两者比例不同时会提示「拉伸会变形，想保住形状就换等比填满」 |

左边是画布预览，改任何参数立刻刷新；棋盘格表示画布上没被图片盖到的部分。底部的 **生成并铺到画布** 产出一张与画布等大的图片层。

---

### Agent 面板（右侧栏切换而来）

| 控件 | 作用 |
| --- | --- |
| **模型设置** | 展开配置区：选接口、选或手填模型、选思考档位、填 API Key、写**自定义提示词**，点「保存设置」写入本机 |
| **自定义提示词** | 固定你个人的排版习惯，如“分镜留白统一 80”“气泡文字一律留空”。不能推翻内置硬规则（分镜之间必须留白、不许照抄参考图里的文字） |
| **限定范围** | 在画布上拖出一块区域，Agent 只在该区域内新增内容；已有范围时按钮变为「重划范围」 |
| **属性** | 切回属性检查器 |
| **预置指令** | 面板空白时显示的几条常用指令，点一下直接执行 |
| **输入框** | 用中文描述想要的排版，Enter 发送、Shift+Enter 换行 |
| **发送** | 同 Enter。执行过程与结果逐条显示在面板里 |
| **参考图** | 把漫画图拖进面板、或 `Ctrl+V` 粘贴，出现缩略图后写一句要求再发送，即可让视觉模型复刻排版；右侧「移除」可取消 |
| **思考档位** | 默认 / 关闭 / 低 / 高 / 最高五档，按接口映射成各自的推理参数 |
| **作用范围条** | 已框定范围时显示尺寸与起点，右侧可「取消限制」 |

> Agent 只调用白名单内的排版操作，且每步都进撤销历史，`Ctrl+Z` 可整段退回。

---

## 六、右侧：画布内容（层级列表）

属性面板右上角的 **画布内容** 按钮切过来。列出**当前页**的全部对象，一行一个，**最上面一行是最顶层**，顺序就是叠放顺序。列表可滚动。

列表自上而下是**顶层 → 底层**。每行内容：

| 位置 | 内容 |
| --- | --- |
| 左侧色标 | 蓝 **分镜** / 橙 **图片层·贴纸** / 绿 **气泡** |
| 中间 | 自定义名字；没起过名就显示自动描述（大小、形状、文字摘要） |
| 行尾悬停 | **改名** 按钮 |
| 行尾常驻 | **⇈ 置于顶层**、**↑ 上移一层**、**↓ 下移一层**、**⇊ 置于底层** |

### 改叠放顺序

- **拖动整行**：拖到目标行的上/下半区，会出现一条高亮线提示落点，松手即生效
- **四个按钮**：⇈ 置顶 / ↑ 上移 / ↓ 下移 / ⇊ 置底

属性面板底部也有同样一排按钮，不必切到列表。

### 命名

**双击名字**直接改，或者点行尾 **改名**。清空则恢复自动描述。名字存进项目文件。

### 分组

| 控件 | 作用 |
| --- | --- |
| **整理分组 / 退出整理** | 切换整理模式。进入后每行左侧出现勾选框 |
| **全选 / 全不选** | 一次勾上全部或全部取消 |
| **建成一组(N)** | 把勾选的项归入一个新分组，默认名「分组 N」 |
| **移出分组** | 把勾选的成员从它所在的组里拿出来，不影响画布 |
| **▶ / ▼** | 折叠或展开该组 |
| 分组标题行悬停的 **改名** | 给分组命名，例如「第一幕」 |
| 分组标题行悬停的 **解散** | 拆掉分组，**里面的内容都还在** |

一个对象同时只属于一个组：把已在组里的对象再建成新组，它会从旧组移出来。**分组只影响列表怎么排，不改动画布叠放顺序。**

**为什么需要它**：叠放顺序一旦被打破（比如铺满一层图盖住整页），画布上就再也戳不到下面的分镜和气泡。在列表里点一下名字即可选中，属性面板立刻切到那个对象，改完再把它 **置底**，下层就露出来了。

> 老项目没有层序、命名、分组信息时按默认顺序推导：分镜在最下 → 图片层与贴纸 → 气泡在最上，和以前的表现一致。

---

## 七、最右侧：胶片栏

按顺序排列所有页面，顺序即漫画顺序。缩略图里画的是页面真实内容（分镜、图片层、气泡、贴纸），方便翻页时辨认。

| 控件 | 作用 |
| --- | --- |
| **页面缩略图** | 单击切换到该页。**直接拖动可以调整整册顺序**，拖到目标页的上/下半区会亮一条提示线。把左侧「已导入图片」的素材拖上来，会在这一页后面插一张新页；拖到列表空白处则追加到末尾 |
| **滚轮** | 滚动列表，**滚到两端会自动翻页** |
| **▲ 上一页 / ▼ 下一页** | 切页 |
| **⧉ 复制本页** | 把当前页原样复制一份插在后面，并切到新页 |
| **＋ 新增空白页** | 在末尾加一页空白页 |
| **↥ 上移该页 / ↧ 下移该页** | 调整当前页在整册中的顺序（拖拽之外的备用方式） |
| **✕ 删除该页** | 删除当前页（仅剩一页时不可删）。也可以先点一下该页缩略图，再按 `Delete` |

### 复制本页带走了什么

分镜、气泡、图片层与贴纸、**层级顺序**、你给对象起的**名字**、**分组关系**，全都复制过去。

但每个对象都会**换成新 id**，所以副本和原页是真正独立的两页：在副本上删内容、改名、调层序，原页完全不受影响。复制完自动切到新页。

适合：一格分镜要画几个不同表情，或者同一版式连续出几页。

---

## 八、底部状态栏

| 位置 | 内容 |
| --- | --- |
| 左 | 当前页 / 总页数、画布尺寸 |
| 中 | 操作提示（如"已创建分镜"），点 **历史** 可翻看最近消息 |
| 右 | 当前页的分镜数与文字数 |

---

## 九、小窗

| 小窗 | 怎么开 | 里有什么 |
| --- | --- | --- |
| **填字区编辑器** | 左侧「导入自定义对话框」，或者在画布上选中气泡后点「编辑填字区」 | 框选填字区域、预览文字、调字体与不透明度；可 **应用到气泡**、**保存为预设**、**保存并存入预设库** |
| **预设库** | 左侧「预设库」 | 列出 `presets/` 里的文件，可载入、删除、下载 JSON |
| **整册版式模板** | 顶部「模板」 | 列出 `templates/` 里的模板，可 **从模板新建**、删除；底部把当前项目 **保存为模板** |
| **贴纸面板** | 顶部「贴纸」 | 四个内置分组共 26 个贴纸，外加 **自定义** 分组：**导入贴纸图片**（可多选）、**保存到贴纸库**（写进项目目录 `stickers/`）、**从贴纸库载入**、**打开文件夹**；贴纸上悬停可单张删除。单击加到画布，可连续添加，底部显示已添加数量 |
| **导入图片** | 顶部「导入图片」 | 多选或拖入图片，可排序、按文件名排序，点「加入素材库」导入。**导入只进素材库，不生成胶片页**；把它拖到右侧胶片栏才会变成一页 |

---

## 十、快捷键

| 按键 | 作用 |
| --- | --- |
| `Ctrl + Z` | 撤销 |
| `Ctrl + Shift + Z` 或 `Ctrl + Y` | 重做 |
| `Delete` / `Backspace` | **焦点在胶片栏**时删除当前页；否则删除画布上选中的对象 |
| `Enter` | 多边形取点时闭合 |
| `Esc` | 取消当前取景 / 退出扣选工具 / 关闭小窗 |

---

## 十一、两个"保存"的区别

新手最容易混的就是这里：

| | 存什么 | 存在哪 | 什么时候用 |
| --- | --- | --- | --- |
| **气泡预设**（左侧） | 单个对话框的外观 | `presets/` | 同一套对话框反复用 |
| **整册模板**（顶部模板） | 整册排版，不含图片 | `templates/` | 换一册继续画，只换画面 |
| **项目文件**（保存项目） | 这一册的全部内容，含图片 | 你选的位置 | 存档、交付 |

**预设管"对话框长什么样"，模板管"这一页怎么排"，项目管"这一册的全部"。**
