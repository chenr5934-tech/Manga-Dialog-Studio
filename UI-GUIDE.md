# 界面按钮速查

按区域列出每个按钮的作用。界面分四大块：顶部工具栏、左侧预设栏、中间画布、右侧属性栏与胶片栏。

---

## 一、顶部第一行：模式与取景工具

| 按钮 | 作用 |
| --- | --- |
| **漫画对话工坊** | 产品名，非按钮 |
| **分镜模式** | 切到分镜工作流。此模式下显示取景工具与底图色 |
| **对话编辑** | 切到气泡编辑工作流。此模式下显示气泡快捷按钮。**切换时会自动收起取景工具**，避免画布停在锁定态 |
| **矩形扣选** | 分镜模式下出现。在画布上拖拽扣出矩形分镜。开启后画布锁定，已有分镜与气泡不可拖动 |
| **多边形扣选** | 分镜模式下出现。单击逐个取点，满 3 点后可按 Enter、点「完成闭合」或回到起点收口 |
| **底图色** | 分镜模式下出现。取色器，决定页面底色（有原稿底图时，原稿铺在底色之上） |
| **+ 圆角气泡** | 对话编辑下出现。在当前页加一个圆角矩形气泡 |
| **+ 椭圆气泡** | 对话编辑下出现。在当前页加一个椭圆气泡 |
| **新建预设** | 对话编辑下出现。打开填字区编辑器，从空白开始做一个对话框预设 |

---

## 二、顶部第二行：项目操作

| 按钮 | 作用 |
| --- | --- |
| **项目名输入框** | 改项目名，会影响导出文件名 |
| **撤销 / 重做** | 逐步回退或恢复编辑。快捷键 `Ctrl+Z` / `Ctrl+Shift+Z`（或 `Ctrl+Y`） |
| **保存项目** | 保存整册项目（含图片）。首次会让你选存放位置 |
| **另存为** | 换一个位置或换一个名字保存 |
| **导入图片** | **按顺序多选导入漫画原稿**，每张图片生成一个页面，画布尺寸与图片一致 |
| **模板** | 打开**整册版式模板**小窗：保存当前排版，或从模板新建后只换画面 |
| **加载项目** | 打开已保存的项目文件，继续编辑 |
| **画布设置** | 展开面板：改画布尺寸／套用 A4、A3 预设，改完点「应用画布」 |
| **分镜布局** | 展开面板：**新建分镜**、**切割画布**（按行列均分）、**手绘分镜**（同矩形扣选）、**16 倍数尺寸**吸附，改完点「应用」 |
| **批量样式** | 展开面板：把圆角、边框粗细等样式**一次性应用到全部分镜** |
| **文字** | 展开面板：**新增文字**、**删除选中内容** |
| **导出** | 展开面板，见下方「导出面板」 |
| **亮 / 暗** | 切换浅色／深色主题，选择会被记住 |

### 导出面板

| 按钮 | 输出 |
| --- | --- |
| **导出 PNG（当前页）** | 当前这一页一张 PNG |
| **导出 PDF（全部页）** | 全部页合并成一个 PDF，按页面顺序 |
| **导出图片 ZIP（全部页）** | 全部页各一张 PNG，打包成一个 ZIP，包内按 `001_、002_` 编号 |
| **1x / 2x** | ZIP 导出的图片倍率。2x 更清晰、文件更大 |

---

## 三、左侧栏：对话框预设

| 按钮 | 作用 |
| --- | --- |
| **导入对话框图** | 选一张对话框图片（PNG 透明底最佳），在图上框出填字区域，存成预设 |
| **编辑填字区** | 先在画布上选中一个气泡，再点这里调整它的填字区域、字体与不透明度 |
| **预设卡片** | **单击**加到画布中央；**拖到画布**上放到指定位置 |
| **保存预设** | 把当前整套自定义预设写进项目目录的 `presets/`，只问一个文件名 |
| **预设库** | 打开预设库小窗：浏览文件夹里的预设文件，可载入或删除，也能下载 JSON |

> 预设是**单个对话框的外观模板**（底图、填字区、字体、不透明度），不含文字内容。
> 整册排版请用顶部的「模板」。

---

## 四、中间画布区

| 控件 | 作用 |
| --- | --- |
| **- / 缩放滑块 / +** | 调整画布显示比例（10% – 100%），只影响显示，不影响导出 |
| **扣选提示条** | 取景时出现在画布上方，说明当前状态，右侧有 **退出扣选** |
| **Enter 提示条** | 多边形取满 3 点后出现，含 **完成闭合** 与 **重来** |
| **选中手柄** | 选中分镜或气泡后出现。拖角缩放、拖边改尺寸、顶部手柄旋转（分镜按 5° 吸附） |
| **顶点手柄** | 多边形分镜的每个顶点都是可拖拽的，拖动即可改形状 |

---

## 五、右侧：属性检查器

选中什么就显示什么，内容随选中对象变化。

| 对象 | 可调项 |
| --- | --- |
| **分镜** | 坐标与尺寸、倾斜角度、边框粗细与颜色、圆角、间隙、**图像来源**（导入图片、打开手动裁剪）、多边形分镜另有 **转为矩形分镜** |
| **气泡** | 内容文字、形状、背景、排版方向、字体字号颜色、文字描边、**尺寸滑条**、**不透明度**、填字区域 |

### 手动裁剪窗口

| 控件 | 作用 |
| --- | --- |
| **拖拽内部** | 移动取景框 |
| **四角方块手柄** | 自由缩放取景框（保持分镜比例） |
| **四边圆点手柄** | 单独推动某条边 |
| **回到当前裁剪** | 恢复到已保存的裁剪位置 |
| **匹配比例最大区域** | 一键取最大可用区域 |
| **清除裁剪** | 移除裁剪，恢复原图完整显示 |
| **应用裁剪** | 写入并关闭 |

---

## 六、最右侧：胶片栏

按顺序排列所有页面，顺序即漫画顺序。

| 控件 | 作用 |
| --- | --- |
| **页面缩略图** | 单击切换到该页。滚轮滚动列表，**滚到两端会自动翻页** |
| **▲ 上一页 / ▼ 下一页** | 切页 |
| **＋ 新增页面** | 在末尾加一页空白页 |
| **↥ 上移该页 / ↧ 下移该页** | 调整当前页在整册中的顺序 |
| **✕ 删除该页** | 删除当前页（仅剩一页时不可删） |

---

## 七、底部状态栏

| 位置 | 内容 |
| --- | --- |
| 左 | 当前页 / 总页数、画布尺寸 |
| 中 | 操作提示（如"已创建分镜"），点 **历史** 可翻看最近消息 |
| 右 | 当前页的分镜数与文字数 |

---

## 八、小窗

| 小窗 | 怎么开 | 里有什么 |
| --- | --- | --- |
| **填字区编辑器** | 左侧「新建预设」或「编辑填字区」 | 框选填字区域、预览文字、调字体与不透明度；可 **应用到气泡**、**保存为预设**、**保存并存入预设库** |
| **预设库** | 左侧「预设库」 | 列出 `presets/` 里的文件，可载入、删除、下载 JSON |
| **整册版式模板** | 顶部「模板」 | 列出 `templates/` 里的模板，可 **从模板新建**、删除；底部把当前项目 **保存为模板** |
| **导入图片** | 顶部「导入图片」 | 多选或拖入图片，可排序、按文件名排序，选追加或替换后导入 |

---

## 九、快捷键

| 按键 | 作用 |
| --- | --- |
| `Ctrl + Z` | 撤销 |
| `Ctrl + Shift + Z` 或 `Ctrl + Y` | 重做 |
| `Delete` / `Backspace` | 删除选中对象 |
| `Enter` | 多边形取点时闭合 |
| `Esc` | 取消当前取景 / 退出扣选工具 / 关闭小窗 |

---

## 十、两个"保存"的区别

新手最容易混的就是这里：

| | 存什么 | 存在哪 | 什么时候用 |
| --- | --- | --- | --- |
| **气泡预设**（左侧） | 单个对话框的外观 | `presets/` | 同一套对话框反复用 |
| **整册模板**（顶部模板） | 整册排版，不含图片 | `templates/` | 换一册继续画，只换画面 |
| **项目文件**（保存项目） | 这一册的全部内容，含图片 | 你选的位置 | 存档、交付 |

**预设管"对话框长什么样"，模板管"这一页怎么排"，项目管"这一册的全部"。**
