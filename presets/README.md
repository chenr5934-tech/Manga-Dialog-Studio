# 预设库文件夹

这里是对话框预设的存放目录，界面左侧的 **预设库文件夹** 按钮会读写这个目录。

- 保存预设：把当前所有自定义预设写成一个 `.json` 文件放在这里
- 载入预设：从这里的文件读回预设，同名预设以文件中的版本为准
- 换电脑或做新一册时，把这个目录一起拷过去即可

每个 `.json` 文件都是一整套预设，可以直接复制给别人、放进网盘或版本库归档。
文件名不要包含 `/ \ ..` 等字符，服务端会拒绝这类名称。
