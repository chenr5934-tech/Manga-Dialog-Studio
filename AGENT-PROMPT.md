# 漫画排版提示词

这份提示词**已经内置**在工具的「模型设置 → 自定义提示词」里，打开就能用，**不需要手动复制任何东西**。

## 怎么找到它

1. 左侧工具栏「辅助」组 → 点 **智能排版**
2. 面板右上角点 **模型设置** 展开
3. 拉到 **自定义提示词（可选）**——里面已经填好了这份提示词

想改就当场改；改坏了点右上角的 **恢复默认** 按钮，一键回到这一份。

## 它写了什么

- **11 种可执行操作**的完整清单：分镜切分、矩形/椭圆/多边形分镜、批量样式、气泡、页面底色等
- **四种常用版式的坐标公式**——2×2、上1下2、上2下1、三行竖排，都给出了可直接代入的算式
- **跨格主格与斜切格**的配比建议
- **分格节奏、构图取景、气泡落位、留白**的经验值
- **批量风格的取值建议**：圆角、倒角、内边距各取短边的百分之几
- **硬性约束与自检清单**

其中坐标公式是验算过的：仓库里的 `scripts/check-layout-formulas.mjs` 会在 A4 竖版、条漫竖版、横版三种画幅上逐组检查——格子之间不重叠、不越界、间隙正确。改公式后跑一下就能确认没算错。

## 和内置规则的关系

提示词分两层，叠加生效：

- **内置规则**（改不掉）：输出 JSON 的格式、坐标必须在画布内、只许用白名单里的操作、分镜之间必须留白、不许照抄参考图里的文字
- **这份提示词**（随便改）：具体怎么排——切几格、格子多大、气泡放哪

也就是说，改了这份提示词不会破坏工具的基本约定，只是改变排版风格。

## 六项排版技能

除了这份提示词，还有六项技能会在需要时自动加载，不必在提示词里重复交代：

分格节奏、格子构图与取景、对话气泡布局、视线引导、留白与呼吸、参考图复刻。

Agent 每一轮回复下面都会写明这轮启用了哪些技能。

## 调试建议

- 坐标总是差一点：把「思考档位」调到 **高** 或 **最高**
- 想固定某套版式：把它的公式直接写进自定义提示词，比让模型现算稳得多
- 一次只让它排一页，比一次排整册可靠

## 实现位置

- 提示词正文：`src/lib/defaultPrompt.ts`
- 内置规则与技能加载：`src/lib/agent.ts`、`src/lib/agentSkills.ts`

改动提示词正文后，记得同步更新 `src/lib/defaultPrompt.ts` 里的常量。