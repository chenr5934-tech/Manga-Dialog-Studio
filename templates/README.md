# 项目模板文件夹

这里存放**整册漫画的版式模板**：页面尺寸、分镜位置与形状、气泡的摆位与样式。

保存模板时会**去掉图片数据**，只留版式。所以从模板新建后，画面是空的，
你把新的原稿导入到各个分镜里即可，分镜位置和气泡排布都不用重做。

和 `presets/` 的区别：

| 目录 | 存的是什么 | 用途 |
| --- | --- | --- |
| `presets/` | 单个对话框（气泡）的外观 | 同一款气泡在不同作品里反复用 |
| `templates/` | 整册页面的排版 | 换一册继续画，只替换画面 |

导出、导入与删除都在界面的「模板」小窗里操作。
