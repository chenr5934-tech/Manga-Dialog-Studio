# 安装教程

面向第一次使用、电脑上还没装过开发环境的人。**整个过程只需要做一次，大约 5 分钟。**

装好之后，以后每次使用只需双击一个文件。

---

## 一、准备工作

你需要的：

| 项目 | 说明 |
| --- | --- |
| 电脑 | Windows / macOS / Linux 都可以 |
| 网络 | 只有第一次安装依赖时需要，装完之后可以完全离线使用 |
| 磁盘空间 | 约 165 MB |
| 浏览器 | Chrome、Edge 等现代浏览器 |

不需要注册账号，不需要服务器，**图片全部在你自己的电脑上处理，不会上传到任何地方**。

---

## 二、下载项目

### 方式 A：直接下载（推荐，不需要懂命令行）

1. 打开仓库页面：<https://github.com/chenr5934-tech/Manga-Dialog-Studio>
2. 点右上角绿色的 **Code** 按钮
3. 选择 **Download ZIP**
4. 把下载到的 zip 解压出来

解压后会得到一个文件夹，里面应该能看到 `启动.bat`、`README.md`、`src` 等文件。

> **建议把它放在 D 盘或"文档"这类普通目录**，例如 `D:\MangaDialogStudio`。
> 不要放在 `C:\Program Files` 下，那里需要管理员权限，可能导致安装失败。
> 路径里有中文一般没问题，但纯英文路径最省心。

### 方式 B：用 git 克隆（已经装了 git 的人）

```bash
git clone https://github.com/chenr5934-tech/Manga-Dialog-Studio.git
```

---

## 三、安装 Node.js

这是唯一需要"安装软件"的一步。本工具是用 Node.js 运行的，所以电脑上必须有它。

### Windows

1. 打开 <https://nodejs.org/zh-cn>
2. 下载 **LTS 版本**（长期支持版，页面左侧那个按钮）
3. 双击安装包，**一路点"下一步"即可**，不需要改任何选项
4. 安装完成后**重启一次电脑**（让系统识别新的环境变量）

### macOS

两种方式任选：

```bash
# 方式一：官网下载安装包
# 打开 https://nodejs.org/zh-cn ，下载 LTS 版 .pkg 双击安装

# 方式二：如果你装了 Homebrew
brew install node
```

### Linux

```bash
# Debian / Ubuntu
sudo apt update && sudo apt install -y nodejs npm

# 或用 nvm 管理版本（推荐，能装到较新的版本）
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
nvm install --lts
```

### 验证是否装好

打开命令行（Windows 按 `Win + R`，输入 `cmd` 回车），输入：

```bash
node -v
```

如果显示类似 `v20.11.0` 的版本号，就成功了。

**要求 v18 或更高版本。** 如果显示的版本低于 18，请到官网下载新的 LTS 版覆盖安装。

如果提示"不是内部或外部命令"，说明还没装好，或者装完没有重启电脑。

---

## 四、启动

### Windows

进入项目文件夹，**双击 `启动.bat`**。

### macOS / Linux

打开终端，进入项目文件夹，运行：

```bash
cd /你的/项目/路径
node scripts/launcher.mjs
```

> 启动器已经按平台处理了打开浏览器的方式（macOS 用 `open`，Linux 用 `xdg-open`）。
> Windows 路径已验证；macOS / Linux 使用同一套 Node 启动器，若遇到问题欢迎反馈。

### 首次启动会发生什么

会出现一个黑色命令行窗口，依次显示三步：

```
  ==========================================
    漫画对话工坊   Manga Dialog Studio
  ==========================================

  [1/3] 首次运行，正在安装依赖，请稍候...
  [2/3] 检测到源码更新，正在构建界面...
  [3/3] 正在启动本地服务...

  漫画对话工坊已启动
  地址: http://127.0.0.1:8737/
  按 Ctrl+C 停止服务
```

**首次需要 1～3 分钟**（要下载约 200 个依赖包，取决于网速）。
之后每次启动都是**几秒钟**，因为不用再安装和构建。

看到"已启动"之后，浏览器会自动打开工具界面。如果没自动打开，手动在浏览器地址栏输入窗口里显示的那个地址即可。

**这个黑色窗口不要关掉**，它就是工具的服务程序。关掉窗口工具就停止运行了。

---

## 五、日常使用

| 操作 | 做法 |
| --- | --- |
| 启动 | 双击 `启动.bat` |
| 停止 | 在黑色窗口里按 `Ctrl + C`，或直接关掉窗口 |
| 再次打开界面 | 服务还在运行时，浏览器访问 `http://127.0.0.1:8737/` 即可 |

工具会把界面地址固定在本机 `127.0.0.1`，**只有你自己的电脑能访问，同一局域网的其他设备也访问不到**，可以放心使用。

---

## 六、常见问题

### 双击 `启动.bat` 后窗口一闪而过

说明启动过程中出错了。请手动排查：

1. 在项目文件夹里按住 `Shift` 右键，选择"在此处打开 PowerShell 窗口"或"命令提示符"
2. 输入 `启动.bat` 然后回车
3. 这样窗口不会自动关闭，就能看到具体报错信息

最常见的原因是 **Node.js 没装好**，或装完没重启电脑。

### 提示"未检测到 Node.js"

回到第三步重新安装，装完**重启电脑**再试。

### 卡在"正在安装依赖"

这是网速问题，耐心等。如果超过 10 分钟没动静，或明确报错，可以换成国内镜像源再试：

```bash
npm config set registry https://registry.npmmirror.com
```

然后重新双击 `启动.bat`。

### 浏览器没自动打开

看命令行窗口里显示的地址（类似 `http://127.0.0.1:8737/`），手动复制到浏览器打开。

### 提示端口被占用怎么办

不用管，**工具会自动换到下一个可用端口**（比如 8738），你看窗口里实际显示的地址就行。

注意：如果你之前启动过一次但没关，再次双击 `启动.bat` 会新开一个实例并用新端口。两个实例互不干扰，但建议保留一个就好。

### 页面打不开或者一片空白

1. 先确认黑色窗口还开着，并且显示"已启动"
2. 确认地址栏端口号和窗口里显示的一致
3. 按 `Ctrl + F5` 强制刷新

### 换台电脑怎么用

把整个项目文件夹拷过去，在新电脑上装好 Node.js，双击 `启动.bat` 即可。或者直接在新电脑上重新下载一遍。

### 怎么更新到最新版

**方式 A（用 git 克隆的）**

```bash
git pull
```

**方式 B（下载 ZIP 的）**

重新下载一遍 ZIP，解压覆盖。

两种情况都不需要手动删除 `node_modules`——启动脚本会自动检测源码有没有更新，需要时重新构建。

### 怎么彻底卸载

1. 确认黑色窗口已关闭
2. 直接删除整个项目文件夹即可

它不会写注册表，不会开机自启，不会在系统里留下其他东西。

唯一留在系统里的是你自己安装的 Node.js，如果别的软件不用它，从"控制面板 → 程序和功能"里卸载即可。

### 我的图片会被上传吗

不会。这是一个**纯本地**工具：界面跑在你自己的电脑上，图片只在浏览器内存里处理，保存项目时写入你选择的本机路径。没有任何联网上传行为。

---

## 七、下一步

安装完成后，打开 `README.md` 了解具体功能与用法：

- 怎么**批量导入**漫画原稿并按顺序排页
- 怎么用**拖拽**的方式加点对话框
- 怎么**框选填字区域**做自己的对话框预设
- 怎么调**气泡不透明度**
- 分镜模式怎么扣区域、怎么导出**多图 ZIP**

---

## 附：给开发者的本地调试

```bash
npm run dev          # 热更新开发服务器（默认 5173 端口）
npm run build        # 类型检查 + 生产构建
node scripts/e2e-check.mjs    # 端到端验收（需先启动服务）
HEADFUL=1 node scripts/e2e-export.mjs   # 导出链路验收，含真实浏览器下载
```
